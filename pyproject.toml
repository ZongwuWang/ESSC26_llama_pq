[project]
name = "edgepq-artifact"
version = "0.1.0"
description = "Artifact environment for EdgePQ CPU LLM inference"
requires-python = ">=3.10"
dependencies = [
  "accelerate>=0.30,<2",
  "datasets>=2.18,<5",
  "huggingface-hub>=0.27,<2",
  "matplotlib>=3.8,<4",
  "numpy>=1.26,<3",
  "torch>=2.2,<3",
  "transformers>=4.40,<6",
]

[tool.uv]
package = false
